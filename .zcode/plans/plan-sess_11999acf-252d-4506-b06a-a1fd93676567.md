Fix Figma MCP + renovar Context7 API key

1. Editar C:\Users\johan\.zcode\cli\config.json:
   - Reemplazar el servidor "Figma" (usa @figma/mcp-server, paquete inexistente en npm — causa del fallo) por:
     "figma": { "type": "stdio", "command": "cmd", "args": ["/c","npx","-y","figma-developer-mcp","--stdio"], "env": { "FIGMA_API_KEY": "<token figd_ existente>" }, "timeoutMs": 60000 }
   - Reutilizar el personal access token que ya creó el usuario.

2. Actualizar CONTEXT7_API_KEY en Windows:
   - Ejecutar: setx CONTEXT7_API_KEY "ctx7sk-826e5950-f2d7-4501-bff3-3da838d96f07"
   - Esto actualiza HKCU\Environment (mismo mecanismo con que se guardó la key anterior).
   - Fallback si el sandbox bloquea setx: dar al usuario el comando para ejecutarlo manualmente.

3. Verificar:
   - Validar JSON del config.
   - Ejecutar cmd /c npx -y figma-developer-mcp --help para confirmar que el paquete existe y arranca en Windows (también pre-cachea npx).

4. Instruir reinicio completo de ZCode (env vars + MCP se cargan al inicio) y verificación en Settings → MCP; probar Context7 en conversación nueva.

5. Actualizar memorias: paquete Figma correcto (figma-developer-mcp), key Context7 renovada 2026-08-24.